export type AppTheme = {
  colors: {
    primary: string;
    lightPrimary: string;
    background: string;
    lightGray: string;
    gray: string;
    darkGray: string;
    mediumGray: string;
    black: string;
    white: string;
    red: string;
  };
  font: {
    sizes: {
      smallest: string;
      small: string;
      regular: string;
      big: string;
      biggest: string;
    };
  };
};
